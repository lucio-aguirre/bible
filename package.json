{
  "name": "bible",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && push-dir --dir=dist --branch=gh-page"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.30",
    "push-dir": "^0.4.1",
    "svelte": "^3.44.0",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "sql.js-httpvfs": "^0.8.11"
  }
}
